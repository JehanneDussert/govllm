# Blocks npm install/update from resolving versions published < 7 days ago.
# Does NOT apply to npm ci (locked versions bypass resolution).
# Protection is effective only when devs run npm >= 11.10.0 locally
# at the time they add or update dependencies.
min-release-age=7

# CI role: enforce engine constraints (node + npm versions), not the age gate.
engine-strict=true
